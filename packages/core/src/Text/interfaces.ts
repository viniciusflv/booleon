import {
  ContainerProps,
  CursorProps,
  MarginProps,
  TransformProps,
  TransitionProps,
  FontProps,
} from '../css';

type Props =
  & FontProps
  & ContainerProps
  & CursorProps
  & MarginProps
  & TransformProps
  & TransitionProps;

export default Props;