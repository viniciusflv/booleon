import {
  ContainerProps,
  CursorProps,
  FontProps,
  MarginProps,
  TransformProps,
  TransitionProps,
} from '../css';

type Props = FontProps &
  ContainerProps &
  CursorProps &
  MarginProps &
  TransformProps &
  TransitionProps;

export default Props;
