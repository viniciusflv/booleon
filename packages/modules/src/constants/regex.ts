export const VAR = 'var_(.*)';
export const NUMBER = `(${VAR}|\\d+|neg_(\\d+))`;
export const LOWER_CASE = '[a-z]+';
export const LETTER = '[A-z]+';
export const HEX_COLOR = '[A-Fa-f0-9]{6}|[A-Fa-f0-9]{3}';
export const UNIT_NUMBER = `${NUMBER}\\$|${NUMBER}${LOWER_CASE}`;
export const FRACTIONS = `(${UNIT_NUMBER}|auto).*`;
export const SIZE = `${UNIT_NUMBER}|${NUMBER}`;
export const RGB_COLOR = `${NUMBER}_${NUMBER}_${NUMBER}_${NUMBER}|${NUMBER}_${NUMBER}_${NUMBER}`;
export const COLOR = `${RGB_COLOR}|${HEX_COLOR}|${VAR}`;
export const PREFIX = `((${LETTER})__)(.*)`;
