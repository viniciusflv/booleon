import { Border } from '../../packages/core/src/core';
import React, { FC } from 'react';

export default {
  title: 'Core | Border',
  component: Border,
};

export const Default: FC = () => <Border />;
