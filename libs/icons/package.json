{
  "name": "@booleon/icons",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "emit": "tsc --declaration --emitDeclarationOnly --outDir dist",
    "build": "npm run parse && node ./esbuild.js && npm run emit",
    "parse": "svj -i ./src/assets -d ./src -r"
  },
  "devDependencies": {
    "svj": "^1.2.0",
    "@configs/eslint": "*",
    "@configs/babel": "*",
    "@configs/jest": "*",
    "@configs/typescript": "*"
  }
}
